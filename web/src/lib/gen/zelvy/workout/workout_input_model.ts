// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.6.1
//   protoc               unknown
// source: zelvy/workout/workout_input_model.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import {
  WorkoutActivityType,
  workoutActivityTypeFromJSON,
  workoutActivityTypeToJSON,
} from "./workout_activity_type_enum";

export const protobufPackage = "zelvy.workout";

export interface WorkoutInputModel {
  activityType: WorkoutActivityType;
  name?: string | undefined;
  kcalBurned: number;
  duration: number;
}

function createBaseWorkoutInputModel(): WorkoutInputModel {
  return { activityType: 0, name: undefined, kcalBurned: 0, duration: 0 };
}

export const WorkoutInputModel: MessageFns<WorkoutInputModel> = {
  encode(message: WorkoutInputModel, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.activityType !== 0) {
      writer.uint32(8).int32(message.activityType);
    }
    if (message.name !== undefined) {
      writer.uint32(18).string(message.name);
    }
    if (message.kcalBurned !== 0) {
      writer.uint32(24).int64(message.kcalBurned);
    }
    if (message.duration !== 0) {
      writer.uint32(33).double(message.duration);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WorkoutInputModel {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWorkoutInputModel();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.activityType = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.kcalBurned = longToNumber(reader.int64());
          continue;
        }
        case 4: {
          if (tag !== 33) {
            break;
          }

          message.duration = reader.double();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WorkoutInputModel {
    return {
      activityType: isSet(object.activityType) ? workoutActivityTypeFromJSON(object.activityType) : 0,
      name: isSet(object.name) ? globalThis.String(object.name) : undefined,
      kcalBurned: isSet(object.kcalBurned) ? globalThis.Number(object.kcalBurned) : 0,
      duration: isSet(object.duration) ? globalThis.Number(object.duration) : 0,
    };
  },

  toJSON(message: WorkoutInputModel): unknown {
    const obj: any = {};
    if (message.activityType !== 0) {
      obj.activityType = workoutActivityTypeToJSON(message.activityType);
    }
    if (message.name !== undefined) {
      obj.name = message.name;
    }
    if (message.kcalBurned !== 0) {
      obj.kcalBurned = Math.round(message.kcalBurned);
    }
    if (message.duration !== 0) {
      obj.duration = message.duration;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<WorkoutInputModel>, I>>(base?: I): WorkoutInputModel {
    return WorkoutInputModel.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<WorkoutInputModel>, I>>(object: I): WorkoutInputModel {
    const message = createBaseWorkoutInputModel();
    message.activityType = object.activityType ?? 0;
    message.name = object.name ?? undefined;
    message.kcalBurned = object.kcalBurned ?? 0;
    message.duration = object.duration ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
