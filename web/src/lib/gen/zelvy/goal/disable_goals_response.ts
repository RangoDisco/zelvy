// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.6.1
//   protoc               unknown
// source: zelvy/goal/disable_goals_response.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { GoalType, goalTypeFromJSON, goalTypeToJSON } from "./goal_type_enum";

export const protobufPackage = "zelvy.goal";

export interface DisableGoalsResponse {
  disabledGoals: GoalType[];
  errorGoals: GoalType[];
}

function createBaseDisableGoalsResponse(): DisableGoalsResponse {
  return { disabledGoals: [], errorGoals: [] };
}

export const DisableGoalsResponse: MessageFns<DisableGoalsResponse> = {
  encode(message: DisableGoalsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    writer.uint32(10).fork();
    for (const v of message.disabledGoals) {
      writer.int32(v);
    }
    writer.join();
    writer.uint32(18).fork();
    for (const v of message.errorGoals) {
      writer.int32(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DisableGoalsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDisableGoalsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag === 8) {
            message.disabledGoals.push(reader.int32() as any);

            continue;
          }

          if (tag === 10) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.disabledGoals.push(reader.int32() as any);
            }

            continue;
          }

          break;
        }
        case 2: {
          if (tag === 16) {
            message.errorGoals.push(reader.int32() as any);

            continue;
          }

          if (tag === 18) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.errorGoals.push(reader.int32() as any);
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DisableGoalsResponse {
    return {
      disabledGoals: globalThis.Array.isArray(object?.disabledGoals)
        ? object.disabledGoals.map((e: any) => goalTypeFromJSON(e))
        : [],
      errorGoals: globalThis.Array.isArray(object?.errorGoals)
        ? object.errorGoals.map((e: any) => goalTypeFromJSON(e))
        : [],
    };
  },

  toJSON(message: DisableGoalsResponse): unknown {
    const obj: any = {};
    if (message.disabledGoals?.length) {
      obj.disabledGoals = message.disabledGoals.map((e) => goalTypeToJSON(e));
    }
    if (message.errorGoals?.length) {
      obj.errorGoals = message.errorGoals.map((e) => goalTypeToJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DisableGoalsResponse>, I>>(base?: I): DisableGoalsResponse {
    return DisableGoalsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DisableGoalsResponse>, I>>(object: I): DisableGoalsResponse {
    const message = createBaseDisableGoalsResponse();
    message.disabledGoals = object.disabledGoals?.map((e) => e) || [];
    message.errorGoals = object.errorGoals?.map((e) => e) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
