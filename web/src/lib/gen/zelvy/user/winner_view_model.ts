// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.6.1
//   protoc               unknown
// source: zelvy/user/winner_view_model.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "zelvy.user";

export interface WinnerViewModel {
  username: string;
  wins: number;
  picture?: string | undefined;
}

function createBaseWinnerViewModel(): WinnerViewModel {
  return { username: "", wins: 0, picture: undefined };
}

export const WinnerViewModel: MessageFns<WinnerViewModel> = {
  encode(message: WinnerViewModel, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.username !== "") {
      writer.uint32(10).string(message.username);
    }
    if (message.wins !== 0) {
      writer.uint32(16).int64(message.wins);
    }
    if (message.picture !== undefined) {
      writer.uint32(26).string(message.picture);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WinnerViewModel {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWinnerViewModel();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.username = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.wins = longToNumber(reader.int64());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.picture = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WinnerViewModel {
    return {
      username: isSet(object.username) ? globalThis.String(object.username) : "",
      wins: isSet(object.wins) ? globalThis.Number(object.wins) : 0,
      picture: isSet(object.picture) ? globalThis.String(object.picture) : undefined,
    };
  },

  toJSON(message: WinnerViewModel): unknown {
    const obj: any = {};
    if (message.username !== "") {
      obj.username = message.username;
    }
    if (message.wins !== 0) {
      obj.wins = Math.round(message.wins);
    }
    if (message.picture !== undefined) {
      obj.picture = message.picture;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<WinnerViewModel>, I>>(base?: I): WinnerViewModel {
    return WinnerViewModel.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<WinnerViewModel>, I>>(object: I): WinnerViewModel {
    const message = createBaseWinnerViewModel();
    message.username = object.username ?? "";
    message.wins = object.wins ?? 0;
    message.picture = object.picture ?? undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
